# Runtime dependencies for users installing PrimePie.
#
# PrimePie currently uses only the Python standard library plus its native
# C++ extension module, so there are no external Python runtime dependencies.
